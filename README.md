# uxmp-gui

# Build commands

### Start dev server

```shell
npm run dev
```

Starts a dev server on `http://localhost:3000/`

### Build project

```bash
npm run build
```

Runs a build and saves the artifacts in `dist/`
